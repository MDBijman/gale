module tuple3
import [std std.io]

fn main: () -> () = \() => {
    let a : (std.ui64, std.ui64, std.ui64) = (3, 5, 7);
    let (b, c, d): (std.ui64, std.ui64, std.ui64) = a;
    std.io.print d;
    ()
};