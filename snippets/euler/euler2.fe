module euler2
import [std std.io]

var prev : std.i32 = 1;
var cur : std.i32 = 1;
var sum : std.i32 = 0;

while (cur <= 4000000) {
	if (cur % 2 == 0) { sum = sum + cur; };

	var tmp : std.i32 = prev;
	prev = cur;
	cur = tmp + cur;
};

std.io.print std.to_string sum;