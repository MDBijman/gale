module fib
import [std std.io std.assert]

let fib: std.ui64 -> std.ui64 = \n => if (n <= 2) { 1 } else { (fib (n - 1)) + (fib (n - 2)) };

let a: std.ui64 = fib 10;

std.io.print a;

std.assert.assert true;
