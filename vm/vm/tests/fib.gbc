mod fib

fn fib ($0: ui64) -> ui64 {
        ui32 $1, 2
        lt $1, $0, $1
        jmpifn $1, @lbl_1
        ui32 $1, 1
        ret $1
@lbl_1  ui32 $1, 1
        sub $2, $0, $1
        call $3, @fib, ($2)
        ui32 $1, 2
        sub $2, $0, $1
        call $4, @fib, ($2)
        add $1, $3, $4
        ret $1
}

fn main ($0: &[&str]) -> ui64 {
        ui32 $1, 0
        idx $2, $0, $1, &str
        call $3, @std:parse_ui64, ($2)
        call $4, @fib, ($3)
        ret $4
}