.const
.code

fn b ($0: u64)
        movc $1, 1
        add $2, $0, $1
        ret $2
endfn

fn a ($0: u64)
        call $1, @b ($0)
        ret $1
endfn

fn main ($0: u64)
        call $1, @a ($0)
        ret $1
endfn